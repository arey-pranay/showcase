import React from "react";
import { Button } from "../ui/button";

const Like = ({}) => {
  return (
    <Button className="bg-primary-50 text-primary-500 border-2 hover:text-white border-primary-500">
      ❤
    </Button>
  );
};

export default Like;
